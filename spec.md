# Specification

## Summary
**Goal:** Replace the AI-generated profile photo in the About section with the real uploaded portrait of Debraj Paul.

**Planned changes:**
- Save the uploaded photo as `debraj-profile.jpg` and use it as the profile image in `AboutSection.tsx`
- Preserve all existing styling (rounded corners, border, shadow) on the photo container

**User-visible outcome:** The About section displays Debraj Paul's real portrait photo instead of a placeholder or AI-generated image.
